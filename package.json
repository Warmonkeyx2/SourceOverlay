{
  "name": "source-overlay-studio",
  "version": "1.0.0",
  "description": "OBS-like web-based overlay editor for any streaming service",
  "private": true,
  "scripts": {
    "setup": "node setup.js",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
